$(document).ready(function() {
    //your code goes inbetween the "{}" 
$('.gallery').cycle({ 
    fx:     'fade', 
    speed:  'fast', 
    timeout: 0, 
    next:   '#next2', 
    prev:   '#prev2' 
});
});